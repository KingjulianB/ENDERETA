__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
data/export/
enderata/.demo_export/
enderata/data/
cache/
enderata/ml_data/
enderata/ml_checkpoints/

# Confidential business document -- the remote repo is public, this
# file is marked "Confidential - Not for distribution" on every page.
# Remove this line only if you deliberately want it published.
ENDERETA_Business_Plan.pdf
Capture_ecran/

# Nationwide vector tiles (147MB, exceeds GitHub's 100MB limit -- see
# enderata/tiles/README.md 'Deployment' section: deployed directly to
# the add-on's persistent /data via SSH instead of shipping in the image.
enderata/tiles/angola.mbtiles
